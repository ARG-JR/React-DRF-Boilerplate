# React-DRF-Boilerplate
A dockerized boilerplate for apps with a ReactJS frontend and a Django/DRF backend using a MySQL database, configured for use with an externally dockerized traefik reverse proxy.
